# Database Configuration Template
# Copy this file to config.properties and fill in your actual database credentials
# Do NOT commit config.properties to version control!

# Database URL (adjust host, port, and database name as needed)
DB_URL=jdbc:postgresql://localhost:5432/localdb

# Database credentials
DB_USER=localuser
DB_PASSWORD=your_password_here

